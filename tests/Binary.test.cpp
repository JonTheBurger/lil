#include <gmock/gmock.h>
#include <gtest/gtest.h>
#include <lil/Binary.hpp>
#include <type_traits>

using namespace lil;

static_assert(std::is_same<uint8_t, BitsToUInt_t<1>>::value, "Compile time bit to type deduction failed");
static_assert(std::is_same<uint8_t, BitsToUInt_t<2>>::value, "Compile time bit to type deduction failed");
static_assert(std::is_same<uint8_t, BitsToUInt_t<3>>::value, "Compile time bit to type deduction failed");
static_assert(std::is_same<uint8_t, BitsToUInt_t<4>>::value, "Compile time bit to type deduction failed");
static_assert(std::is_same<uint8_t, BitsToUInt_t<5>>::value, "Compile time bit to type deduction failed");
static_assert(std::is_same<uint8_t, BitsToUInt_t<6>>::value, "Compile time bit to type deduction failed");
static_assert(std::is_same<uint8_t, BitsToUInt_t<7>>::value, "Compile time bit to type deduction failed");
static_assert(std::is_same<uint8_t, BitsToUInt_t<8>>::value, "Compile time bit to type deduction failed");
static_assert(std::is_same<uint16_t, BitsToUInt_t<9>>::value, "Compile time bit to type deduction failed");
static_assert(std::is_same<uint16_t, BitsToUInt_t<10>>::value, "Compile time bit to type deduction failed");
static_assert(std::is_same<uint16_t, BitsToUInt_t<11>>::value, "Compile time bit to type deduction failed");
static_assert(std::is_same<uint16_t, BitsToUInt_t<12>>::value, "Compile time bit to type deduction failed");
static_assert(std::is_same<uint16_t, BitsToUInt_t<13>>::value, "Compile time bit to type deduction failed");
static_assert(std::is_same<uint16_t, BitsToUInt_t<14>>::value, "Compile time bit to type deduction failed");
static_assert(std::is_same<uint16_t, BitsToUInt_t<15>>::value, "Compile time bit to type deduction failed");
static_assert(std::is_same<uint16_t, BitsToUInt_t<16>>::value, "Compile time bit to type deduction failed");
static_assert(std::is_same<uint32_t, BitsToUInt_t<17>>::value, "Compile time bit to type deduction failed");
static_assert(std::is_same<uint32_t, BitsToUInt_t<18>>::value, "Compile time bit to type deduction failed");
static_assert(std::is_same<uint32_t, BitsToUInt_t<19>>::value, "Compile time bit to type deduction failed");
static_assert(std::is_same<uint32_t, BitsToUInt_t<20>>::value, "Compile time bit to type deduction failed");
static_assert(std::is_same<uint32_t, BitsToUInt_t<21>>::value, "Compile time bit to type deduction failed");
static_assert(std::is_same<uint32_t, BitsToUInt_t<22>>::value, "Compile time bit to type deduction failed");
static_assert(std::is_same<uint32_t, BitsToUInt_t<23>>::value, "Compile time bit to type deduction failed");
static_assert(std::is_same<uint32_t, BitsToUInt_t<24>>::value, "Compile time bit to type deduction failed");
static_assert(std::is_same<uint32_t, BitsToUInt_t<25>>::value, "Compile time bit to type deduction failed");
static_assert(std::is_same<uint32_t, BitsToUInt_t<26>>::value, "Compile time bit to type deduction failed");
static_assert(std::is_same<uint32_t, BitsToUInt_t<27>>::value, "Compile time bit to type deduction failed");
static_assert(std::is_same<uint32_t, BitsToUInt_t<28>>::value, "Compile time bit to type deduction failed");
static_assert(std::is_same<uint32_t, BitsToUInt_t<29>>::value, "Compile time bit to type deduction failed");
static_assert(std::is_same<uint32_t, BitsToUInt_t<30>>::value, "Compile time bit to type deduction failed");
static_assert(std::is_same<uint32_t, BitsToUInt_t<31>>::value, "Compile time bit to type deduction failed");
static_assert(std::is_same<uint32_t, BitsToUInt_t<32>>::value, "Compile time bit to type deduction failed");
static_assert(std::is_same<uint64_t, BitsToUInt_t<33>>::value, "Compile time bit to type deduction failed");
static_assert(std::is_same<uint64_t, BitsToUInt_t<34>>::value, "Compile time bit to type deduction failed");
static_assert(std::is_same<uint64_t, BitsToUInt_t<35>>::value, "Compile time bit to type deduction failed");
static_assert(std::is_same<uint64_t, BitsToUInt_t<36>>::value, "Compile time bit to type deduction failed");
static_assert(std::is_same<uint64_t, BitsToUInt_t<37>>::value, "Compile time bit to type deduction failed");
static_assert(std::is_same<uint64_t, BitsToUInt_t<38>>::value, "Compile time bit to type deduction failed");
static_assert(std::is_same<uint64_t, BitsToUInt_t<39>>::value, "Compile time bit to type deduction failed");
static_assert(std::is_same<uint64_t, BitsToUInt_t<40>>::value, "Compile time bit to type deduction failed");
static_assert(std::is_same<uint64_t, BitsToUInt_t<41>>::value, "Compile time bit to type deduction failed");
static_assert(std::is_same<uint64_t, BitsToUInt_t<42>>::value, "Compile time bit to type deduction failed");
static_assert(std::is_same<uint64_t, BitsToUInt_t<43>>::value, "Compile time bit to type deduction failed");
static_assert(std::is_same<uint64_t, BitsToUInt_t<44>>::value, "Compile time bit to type deduction failed");
static_assert(std::is_same<uint64_t, BitsToUInt_t<45>>::value, "Compile time bit to type deduction failed");
static_assert(std::is_same<uint64_t, BitsToUInt_t<46>>::value, "Compile time bit to type deduction failed");
static_assert(std::is_same<uint64_t, BitsToUInt_t<47>>::value, "Compile time bit to type deduction failed");
static_assert(std::is_same<uint64_t, BitsToUInt_t<48>>::value, "Compile time bit to type deduction failed");
static_assert(std::is_same<uint64_t, BitsToUInt_t<49>>::value, "Compile time bit to type deduction failed");
static_assert(std::is_same<uint64_t, BitsToUInt_t<50>>::value, "Compile time bit to type deduction failed");
static_assert(std::is_same<uint64_t, BitsToUInt_t<51>>::value, "Compile time bit to type deduction failed");
static_assert(std::is_same<uint64_t, BitsToUInt_t<52>>::value, "Compile time bit to type deduction failed");
static_assert(std::is_same<uint64_t, BitsToUInt_t<53>>::value, "Compile time bit to type deduction failed");
static_assert(std::is_same<uint64_t, BitsToUInt_t<54>>::value, "Compile time bit to type deduction failed");
static_assert(std::is_same<uint64_t, BitsToUInt_t<55>>::value, "Compile time bit to type deduction failed");
static_assert(std::is_same<uint64_t, BitsToUInt_t<56>>::value, "Compile time bit to type deduction failed");
static_assert(std::is_same<uint64_t, BitsToUInt_t<57>>::value, "Compile time bit to type deduction failed");
static_assert(std::is_same<uint64_t, BitsToUInt_t<58>>::value, "Compile time bit to type deduction failed");
static_assert(std::is_same<uint64_t, BitsToUInt_t<59>>::value, "Compile time bit to type deduction failed");
static_assert(std::is_same<uint64_t, BitsToUInt_t<60>>::value, "Compile time bit to type deduction failed");
static_assert(std::is_same<uint64_t, BitsToUInt_t<61>>::value, "Compile time bit to type deduction failed");
static_assert(std::is_same<uint64_t, BitsToUInt_t<62>>::value, "Compile time bit to type deduction failed");
static_assert(std::is_same<uint64_t, BitsToUInt_t<63>>::value, "Compile time bit to type deduction failed");
static_assert(std::is_same<uint64_t, BitsToUInt_t<64>>::value, "Compile time bit to type deduction failed");

static_assert(std::is_same<int8_t, BitsToInt_t<1>>::value, "Compile time bit to type deduction failed");
static_assert(std::is_same<int8_t, BitsToInt_t<2>>::value, "Compile time bit to type deduction failed");
static_assert(std::is_same<int8_t, BitsToInt_t<3>>::value, "Compile time bit to type deduction failed");
static_assert(std::is_same<int8_t, BitsToInt_t<4>>::value, "Compile time bit to type deduction failed");
static_assert(std::is_same<int8_t, BitsToInt_t<5>>::value, "Compile time bit to type deduction failed");
static_assert(std::is_same<int8_t, BitsToInt_t<6>>::value, "Compile time bit to type deduction failed");
static_assert(std::is_same<int8_t, BitsToInt_t<7>>::value, "Compile time bit to type deduction failed");
static_assert(std::is_same<int8_t, BitsToInt_t<8>>::value, "Compile time bit to type deduction failed");
static_assert(std::is_same<int16_t, BitsToInt_t<9>>::value, "Compile time bit to type deduction failed");
static_assert(std::is_same<int16_t, BitsToInt_t<10>>::value, "Compile time bit to type deduction failed");
static_assert(std::is_same<int16_t, BitsToInt_t<11>>::value, "Compile time bit to type deduction failed");
static_assert(std::is_same<int16_t, BitsToInt_t<12>>::value, "Compile time bit to type deduction failed");
static_assert(std::is_same<int16_t, BitsToInt_t<13>>::value, "Compile time bit to type deduction failed");
static_assert(std::is_same<int16_t, BitsToInt_t<14>>::value, "Compile time bit to type deduction failed");
static_assert(std::is_same<int16_t, BitsToInt_t<15>>::value, "Compile time bit to type deduction failed");
static_assert(std::is_same<int16_t, BitsToInt_t<16>>::value, "Compile time bit to type deduction failed");
static_assert(std::is_same<int32_t, BitsToInt_t<17>>::value, "Compile time bit to type deduction failed");
static_assert(std::is_same<int32_t, BitsToInt_t<18>>::value, "Compile time bit to type deduction failed");
static_assert(std::is_same<int32_t, BitsToInt_t<19>>::value, "Compile time bit to type deduction failed");
static_assert(std::is_same<int32_t, BitsToInt_t<20>>::value, "Compile time bit to type deduction failed");
static_assert(std::is_same<int32_t, BitsToInt_t<21>>::value, "Compile time bit to type deduction failed");
static_assert(std::is_same<int32_t, BitsToInt_t<22>>::value, "Compile time bit to type deduction failed");
static_assert(std::is_same<int32_t, BitsToInt_t<23>>::value, "Compile time bit to type deduction failed");
static_assert(std::is_same<int32_t, BitsToInt_t<24>>::value, "Compile time bit to type deduction failed");
static_assert(std::is_same<int32_t, BitsToInt_t<25>>::value, "Compile time bit to type deduction failed");
static_assert(std::is_same<int32_t, BitsToInt_t<26>>::value, "Compile time bit to type deduction failed");
static_assert(std::is_same<int32_t, BitsToInt_t<27>>::value, "Compile time bit to type deduction failed");
static_assert(std::is_same<int32_t, BitsToInt_t<28>>::value, "Compile time bit to type deduction failed");
static_assert(std::is_same<int32_t, BitsToInt_t<29>>::value, "Compile time bit to type deduction failed");
static_assert(std::is_same<int32_t, BitsToInt_t<30>>::value, "Compile time bit to type deduction failed");
static_assert(std::is_same<int32_t, BitsToInt_t<31>>::value, "Compile time bit to type deduction failed");
static_assert(std::is_same<int32_t, BitsToInt_t<32>>::value, "Compile time bit to type deduction failed");
static_assert(std::is_same<int64_t, BitsToInt_t<33>>::value, "Compile time bit to type deduction failed");
static_assert(std::is_same<int64_t, BitsToInt_t<34>>::value, "Compile time bit to type deduction failed");
static_assert(std::is_same<int64_t, BitsToInt_t<35>>::value, "Compile time bit to type deduction failed");
static_assert(std::is_same<int64_t, BitsToInt_t<36>>::value, "Compile time bit to type deduction failed");
static_assert(std::is_same<int64_t, BitsToInt_t<37>>::value, "Compile time bit to type deduction failed");
static_assert(std::is_same<int64_t, BitsToInt_t<38>>::value, "Compile time bit to type deduction failed");
static_assert(std::is_same<int64_t, BitsToInt_t<39>>::value, "Compile time bit to type deduction failed");
static_assert(std::is_same<int64_t, BitsToInt_t<40>>::value, "Compile time bit to type deduction failed");
static_assert(std::is_same<int64_t, BitsToInt_t<41>>::value, "Compile time bit to type deduction failed");
static_assert(std::is_same<int64_t, BitsToInt_t<42>>::value, "Compile time bit to type deduction failed");
static_assert(std::is_same<int64_t, BitsToInt_t<43>>::value, "Compile time bit to type deduction failed");
static_assert(std::is_same<int64_t, BitsToInt_t<44>>::value, "Compile time bit to type deduction failed");
static_assert(std::is_same<int64_t, BitsToInt_t<45>>::value, "Compile time bit to type deduction failed");
static_assert(std::is_same<int64_t, BitsToInt_t<46>>::value, "Compile time bit to type deduction failed");
static_assert(std::is_same<int64_t, BitsToInt_t<47>>::value, "Compile time bit to type deduction failed");
static_assert(std::is_same<int64_t, BitsToInt_t<48>>::value, "Compile time bit to type deduction failed");
static_assert(std::is_same<int64_t, BitsToInt_t<49>>::value, "Compile time bit to type deduction failed");
static_assert(std::is_same<int64_t, BitsToInt_t<50>>::value, "Compile time bit to type deduction failed");
static_assert(std::is_same<int64_t, BitsToInt_t<51>>::value, "Compile time bit to type deduction failed");
static_assert(std::is_same<int64_t, BitsToInt_t<52>>::value, "Compile time bit to type deduction failed");
static_assert(std::is_same<int64_t, BitsToInt_t<53>>::value, "Compile time bit to type deduction failed");
static_assert(std::is_same<int64_t, BitsToInt_t<54>>::value, "Compile time bit to type deduction failed");
static_assert(std::is_same<int64_t, BitsToInt_t<55>>::value, "Compile time bit to type deduction failed");
static_assert(std::is_same<int64_t, BitsToInt_t<56>>::value, "Compile time bit to type deduction failed");
static_assert(std::is_same<int64_t, BitsToInt_t<57>>::value, "Compile time bit to type deduction failed");
static_assert(std::is_same<int64_t, BitsToInt_t<58>>::value, "Compile time bit to type deduction failed");
static_assert(std::is_same<int64_t, BitsToInt_t<59>>::value, "Compile time bit to type deduction failed");
static_assert(std::is_same<int64_t, BitsToInt_t<60>>::value, "Compile time bit to type deduction failed");
static_assert(std::is_same<int64_t, BitsToInt_t<61>>::value, "Compile time bit to type deduction failed");
static_assert(std::is_same<int64_t, BitsToInt_t<62>>::value, "Compile time bit to type deduction failed");
static_assert(std::is_same<int64_t, BitsToInt_t<63>>::value, "Compile time bit to type deduction failed");
static_assert(std::is_same<int64_t, BitsToInt_t<64>>::value, "Compile time bit to type deduction failed");

